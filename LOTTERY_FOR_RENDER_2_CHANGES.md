# Lottery for Render 2 — Changes

Only the two documentation corrections requested after Replit's verification were made:

1. Removed the obsolete Render storage guidance that conflicted with PostgreSQL being the authoritative production data store.
2. Corrected the Admin 2FA documentation so it consistently states that 2FA is optional and can be enabled or disabled from the Admin panel.

No application code or feature behavior was changed.
No database data was created, deleted, migrated, or modified.
No production credentials were added.